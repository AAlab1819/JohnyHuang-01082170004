#include <iostream>

using namespace std;

int main()
{
    int n;
    cin>>n;
    int inp, sum=0;
    for(int i=0; i<n; i++){
        cin>>inp;
        if(inp<0)
            sum -= inp;
        else
            sum += inp;
    }
    cout<<sum;
    return 0;
}
