#include <iostream>
#include <string>
using namespace std;

int main()
{
    int n;
    cin>>n;
    int i=1;
    string s;
    while(s.length()<n)
    {
        s+=to_string(i);
        i++;
    }
    cout<<s[n-1];
    return 0;
}
